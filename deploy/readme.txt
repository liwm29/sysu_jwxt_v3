this deploy server is designed to deploy frontend & backend together
the frontend should be released using `npm run build`,which create `/dist`.
for further details, see src code

for some reason , frontend is not finished yet, so ignore this folder.
cd /cmd using cli instead
